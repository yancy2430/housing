<van-tabbar active="{{ active }}" bind:change="onChange" active-color="#2DBE35">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}" info="{{index==4 && ms>0?ms:''}}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
